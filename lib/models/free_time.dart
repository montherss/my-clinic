import 'package:flutter/material.dart';

class DoctorFreeTime{
  String? date ;
  String ? title ;
  String ? note ;
  String ? startedTime ;
  String ? day;
  String ? endTime ;
  int ? remider ;
  DoctorFreeTime({this.date , this.day,this.endTime,this.note,this.remider,this.startedTime,this.title});
}